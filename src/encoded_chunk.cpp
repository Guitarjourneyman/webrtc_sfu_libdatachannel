#include "encoded_chunk.hpp"

namespace ldc_toy {
// The type is intentionally data-only. Parsing from WebSocket/WebTransport
// packets belongs in an adapter so the SFU core remains a plain C++ API.
}

